import SwiftUI

@main
struct OptimizadorMacM4App: App {
    var body: some Scene {
        WindowGroup {
            MainNavigationView()
        }
        .windowStyle(.automatic)
        .defaultSize(width: 1000, height: 700)
    }
}
